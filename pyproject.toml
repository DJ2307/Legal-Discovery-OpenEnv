[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "legal-discovery-env"
version = "1.0.0"
description = "Legal discovery simulation"
dependencies = [
    "openenv-core",
    "fastapi",
    "uvicorn",
    "pydantic"
]
